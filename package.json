{
  "name": "proton-website",
  "version": "1.0.0",
  "description": "A static website for the Proton project",
  "scripts": {
    "start": "serve -s public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
